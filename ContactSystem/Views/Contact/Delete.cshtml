@model ContactModel

@{
    ViewData["Title"] = "Apagar contato";
}

<div class="text-center">
    <h4 class="font-weight-light"><em>Deseja realmente apagar o contato ()?</em></h4>
</div>

<a class="btn btn-outline-success btn-block" role="button" asp-controller="Contact" asp-action="Delete">Confirmar</a>
<a class="btn btn-outline-danger btn-block" role="button" asp-controller="Contact" asp-action="Index">Cancelar</a>